{
  "name": "kimiclaw-db",
  "version": "1.0.0",
  "description": "Kimiclaw Database Module - DuckDB + Batch Buffer",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "test": "jest",
    "dev": "ts-node src/index.ts",
    "ci-check": "ts-node src/ci-checks.ts",
    "prepush": "npm run ci-check && npm run build && npm test",
    "lint": "eslint src/**/*.ts"
  },
  "dependencies": {
    "@types/jest": "^30.0.0",
    "@types/node": "^25.3.0",
    "duckdb": "^1.4.4",
    "jest": "^30.2.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  },
  "devDependencies": {
    "ts-jest": "^29.4.6",
    "eslint": "^8.57.1",
    "@typescript-eslint/eslint-plugin": "^7.0.0",
    "@typescript-eslint/parser": "^7.0.0"
  }
}
